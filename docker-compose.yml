services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      # Mount source for hot reload — don't mount node_modules
      - .:/app
      - /app/node_modules
    env_file:
      - .env
